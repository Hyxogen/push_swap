#ifndef QUICK_SOLVE_H
# define QUICK_SOLVE_H

# include "../../ps_object.h"

void quick_solve(t_ps_object *object, int *arr, size_t size);
int evaluate(t_stack *des, t_stack *origin, int number);
#endif
